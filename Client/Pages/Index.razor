@page "/"
@page "/search/{searchText}"
@page "/{categoryUrl}"
 
<PageTitle>My Ecom</PageTitle>

@if (SearchText == null && CategoryUrl == null)
{
    <FeaturedProducts />
}
else
{
    <ProductList />
}